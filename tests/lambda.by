from __future__ import (
    annotations
)
import asyncio


def a() {
    b = 3
    return lambda x {
        for i in range(b) {
            x += i
        }
        return lambda { x },
    }
}

assert a()(0)[0]() == 3

if lambda {} {
    asyncio.run(async lambda { print("Hello") }())
}

lambda a={}, b={}: a.update(b)
